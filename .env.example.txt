################################################################################
# Arquivo de Exemplo de Variáveis de Ambiente (.env.example)
#
# Crie uma cópia deste arquivo, renomeie-a para **.env**, 
# e preencha com suas credenciais REAIS.
# O arquivo .env NUNCA deve ser enviado para o GitHub.
################################################################################


# ==============================================================================
# 1. CONFIGURAÇÃO GOOGLE CLOUD PLATFORM (GCP)
# Necessário para: BigQuery, Cloud Storage e Autenticação Geral.
# ==============================================================================

# O ID do seu projeto GCP (ex: challenge)
GCP_PROJECT_ID="SEU_ID_DO_PROJETO"

# O nome do bucket onde as imagens das etiquetas estão armazenadas (ex: imagens)
GCP_BUCKET_NAME="NOME_DO_SEU_BUCKET"

# Caminho completo para o arquivo JSON da chave da conta de serviço. 
# Requerida para a Autenticação (ADC). Use aspas duplas, pois o caminho é local.
# Exemplo Windows: "C:\Users\...\chave-de-servico.json"
GOOGLE_APPLICATION_CREDENTIALS="CAMINHO_COMPLETO_PARA_SUA_CHAVE.json"

# Chave da API do Google Vision. Usada na função ocrGoogleVisionApi.
# É uma string de ~39 caracteres (ex: AIzD... )
GOOGLE_VISION_API_KEY="SUA_CHAVE_API_VISION"


# ==============================================================================
# 2. CONFIGURAÇÃO DE ALERTA DE E-MAIL (SMTP)
# Necessário para: O notebook de Ações Estratégicas (03_business_actions).
# ==============================================================================

# Seu e-mail (remetente). O e-mail deve ter 2FA ativada para usar App Password.
GMAIL_USER="SEU_EMAIL_DE_ALERTA@gmail.com"

# Senha de App de 16 dígitos gerada nas configurações de segurança do Google.
GMAIL_APP_PASSWORD="SENHA DE APP DE 16 DIGITOS"

# O endereço de e-mail que receberá os alertas do sistema.
DESTINATARIO_EMAIL="EMAIL_QUE_RECEBE_ALERTAS@dominio.com"